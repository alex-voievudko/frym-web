---
import {
	validationQuestion,
	validationOptions,
} from '../../data/validation-options';

/**
 * Buttons with aria-pressed, not a radiogroup: a radiogroup traps arrow keys
 * and firing on `change` would send a request per arrow press, while adding a
 * submit button puts two steps in front of a one-tap question.
 */
---

<div class="vq" data-vq hidden>
	<div class="vq__head">
		<p class="vq__eyebrow">One quick question</p>
		<h4 class="vq__label" id="vq-label">{validationQuestion}</h4>
	</div>

	<ul class="vq__options" aria-labelledby="vq-label" data-vq-options>
		{
			validationOptions.map((o) => (
				<li>
					<button
						type="button"
						class="vq__option"
						data-option={o.id}
						aria-pressed="false"
					>
						{o.label}
					</button>
				</li>
			))
		}
	</ul>

	<div class="vq__noted" data-vq-noted hidden>
		<p class="vq__noted-label">
			<span class="vq__badge">Noted</span>
			<span data-vq-choice></span>
		</p>
		<p class="vq__thanks" role="status">
			Thanks — this helps us build the right first version.
		</p>
	</div>
</div>

<style>
	.vq {
		display: flex;
		flex-direction: column;
		gap: var(--s-4);
		border-top: 1px solid var(--border-onInverse-strong);
		padding-top: var(--s-5);
		margin-top: var(--s-5);
	}

	/* Tighter than the block's own gap — the eyebrow belongs to the question. */
	.vq__head {
		display: flex;
		flex-direction: column;
		gap: 10px;
	}

	.vq__eyebrow {
		font-family: var(--font-mono);
		font-weight: 500;
		font-size: var(--mono-action);
		letter-spacing: 0.16em;
		text-transform: uppercase;
		color: var(--text-muted);
		line-height: 1;
	}

	.vq__label {
		font-size: var(--type-body-md);
		font-weight: 400;
		line-height: 1.6;
		color: var(--text-onInverse);
	}

	/* Mobile: a stacked list with 44px targets. Wrapping pills from 768. */
	.vq__options {
		display: flex;
		flex-direction: column;
		gap: 0;
	}

	.vq__option {
		display: flex;
		align-items: center;
		width: 100%;
		min-height: 44px;
		padding: 12px 0;
		border-top: 1px solid var(--border-onInverse);
		font-family: var(--font-mono);
		font-weight: 500;
		font-size: var(--mono-action);
		letter-spacing: 0.14em;
		text-transform: uppercase;
		text-align: left;
		color: var(--text-bodyOnInverse);
		line-height: 1.3;
		transition:
			color var(--t-hover) var(--ease),
			border-color var(--t-hover) var(--ease);
	}

	.vq__options li:first-child .vq__option {
		border-top: none;
	}

	@media (hover: hover) and (pointer: fine) {
		.vq__option:hover {
			color: var(--text-onInverse);
		}
	}

	.vq__option:active {
		transform: scale(var(--press-scale));
		transition-duration: var(--t-fast);
	}

	.vq__option[disabled] {
		cursor: default;
		opacity: 0.5;
	}

	.vq__noted {
		display: flex;
		flex-direction: column;
		gap: var(--s-2);
	}

	.vq__noted-label {
		display: flex;
		align-items: center;
		gap: var(--s-2);
		font-size: var(--type-body-md);
		color: var(--text-onInverse);
	}

	.vq__badge {
		border-radius: var(--r-tag);
		padding: 3px 7px;
		background: var(--border-onInverse-strong);
		color: var(--text-onInverse);
		font-family: var(--font-mono);
		font-weight: 500;
		font-size: var(--mono-tag);
		letter-spacing: 0.12em;
		text-transform: uppercase;
		flex: none;
	}

	.vq__thanks {
		font-size: var(--type-body-sm);
		line-height: 1.65;
		color: var(--text-bodyOnInverse);
	}

	@media (min-width: 768px) {
		.vq__options {
			flex-direction: row;
			flex-wrap: wrap;
			gap: var(--s-2);
		}

		.vq__option {
			width: auto;
			min-height: 38px;
			padding: 0 15px;
			border: 1px solid var(--border-onInverse-strong);
			border-radius: var(--r-pill);
		}

		.vq__options li:first-child .vq__option {
			border: 1px solid var(--border-onInverse-strong);
		}

		@media (hover: hover) and (pointer: fine) {
			.vq__option:hover {
				border-color: var(--text-onInverse);
			}
		}
	}
</style>
